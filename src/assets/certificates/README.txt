Add certificate files here (PDF or image) and then update
src/app/services/portfolio.service.ts -> certificates array:

  fileUrl: 'assets/certificates/your-file-name.pdf'

Until a real file is added, leave fileUrl as '[ADD CERTIFICATE]' —
the certificate card will show that as a disabled label instead of a
broken link.
